# RemoteView mobile configuration (optional)
# The mobile app is served by the laptop server, so it normally needs no config.
# Only used when running `npm run dev` on a separate machine:

# VITE_LAPTOP_URL=http://192.168.1.50:8756
